# Development defaults only. Generate strong independent values for deployments.
HMAC_SECRET=local-demo-only-secret-change-before-deploying
POSTGRES_PASSWORD=video-local-password
COUNTER_SHARD_COUNT=128
KAFKA_PARTITIONS=24
